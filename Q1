#include <stdio.h>

int fatorial(int a){
    int i, ret;
    ret = 1;

if(a>1)
{
	ret=fatorial(a-1)+a;
}

    return ret;
}

int main()
{
    printf("soma dos %d primeiros = %d \n\n", 100, fatorial(100));
    return 0;
}
