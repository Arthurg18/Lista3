
#include <stdio.h>

int main()
{
	  int a, b, soma;  
  printf("Digite um numero inteiro: ");
  scanf("%d", &a); //recebe um inteiro e armazena na variável a

	int i;
for( i = 1; i<11; i++)
{
	
	int valor = i*a;
	if(valor > 0)
	{
	printf("valor da tabuada de n = %d \n\n", valor );
    	
	}
}
    
    return 0;
}

